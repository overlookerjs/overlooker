const describePerformance = require('./describe-performance.js');
const warming = require('./warming.js');
const content = require('./content.js');

module.exports = {
  describePerformance,
  warming,
  content,
};
